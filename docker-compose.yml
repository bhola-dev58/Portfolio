services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb+srv://dev_db:Dev_DB@myportfolio.xhnowgo.mongodb.net/portfolio?retryWrites=true&w=majority
      - JWT_SECRET=bhola_portfolio_jwt_secret_2024_very_secure_key
      - PORT=5000
      - ADMIN_EMAIL=bhola.dev58@gmail.com
      - ADMIN_PASSWORD=admin123
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=http://localhost:5000/api
    ports:
      - "8082:80"
    restart: unless-stopped
    depends_on:
      - backend

# To run:
# docker compose up --build -d
